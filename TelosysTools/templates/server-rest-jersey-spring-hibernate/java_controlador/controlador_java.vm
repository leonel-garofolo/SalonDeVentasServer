package ${target.javaPackageFromFolder($SRC)};

import ${ROOT_PKG}.modelo.jpa.${entity.name}Entity;

public interface I${entity.name}Controlador extends IControlador<${entity.name}Entity, java.lang.Long>{
	
}
